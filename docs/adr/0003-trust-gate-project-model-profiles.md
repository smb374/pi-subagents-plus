# Trust-gate project model profiles

Project model profiles are read only from trusted projects, and otherwise the extension uses user model profiles. A model profile controls which provider and model execute subagent prompts, so allowing an untrusted repository to supply one would cross Pi's project-resource trust boundary.
