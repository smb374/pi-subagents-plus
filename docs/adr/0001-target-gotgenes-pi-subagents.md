# Target @gotgenes/pi-subagents exclusively

Pi Subagents Plus is a companion to `@gotgenes/pi-subagents`, not the unrelated `pi-subagents` package by nicobailon. The latter is only a source for agent definitions to adapt; supporting both runtimes would create two distinct integrations and obscure the extension's boundary.
