import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Register the Pi Subagents Plus Pi extension. */
export default function extension(_pi: ExtensionAPI): void {
    // Register tools, commands, shortcuts, and event handlers here.
    // Keep registration synchronous and free of settings I/O or feature initialization.
}
